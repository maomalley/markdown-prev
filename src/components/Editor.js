import React from 'react';

function Editor(props) {
    // addEventListener("input", (event) => {});
    //
    // oninput = (event) => {};

    return (
        <div></div>
    );
}

export default Editor;